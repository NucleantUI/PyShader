# Module style

The optional stub import (ignored by the compiler), `pyshader.sin` as a
qualified call, an annotated module constant derived from another, a chained
comparison, and indexing a vector with positive and negative indices.

```pyshader file="Examples/module_style.py"
```
