# Control flow

`if` / `elif` / `else`, `while`, `for i in range(...)`, `break`, `continue`,
early `return` and nested loops, in one shader. The pointer moves the
highlight.

```pyshader file="Examples/control_flow.py"
```
